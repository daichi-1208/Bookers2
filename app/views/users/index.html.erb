<div class ="container">
	<div class ="row">
<%= render 'books/sidebar', book: Book.new, side_user: @side_user %>

<div class="col-xs-9">
<table class="table table-striped">
<thead>
<tr>
<th></th>
<th>name</th>
<th></th>
<th></th>
</tr>
</thead>

<tbody>
 <% @users.each do |user| %>
 <tr>
 	<td>
 		<%=link_to user_path(user.id) do%>
		<%= attachment_image_tag user, :profile_image, :fill, 150, 150, fallback: 'no-image-icon.jpg' %>
		<%end%>
 	</td>
  <td>
   <%= user.name %>
  </td>
  <td>
   <%= link_to "show", user_path(user.id) %>
  </td>

</tr>
<% end %>
</tbody>
</table>
</div>
</div>
</div>
