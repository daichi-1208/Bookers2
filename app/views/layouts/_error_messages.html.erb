<% if @book.errors.any? %>
  <div class= "alert alert-warning">
  	<%= @book.errors.count %>error
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>