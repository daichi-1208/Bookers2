<div class="container">
	<div class="row">

<%= render 'books/sidebar', book: Book.new, side_user: @side_user %>
<div class="col-xs-9">


<table>
<thead>
<tr>
<th></th>
<th>title</th>
<th>Opinion</th>
</tr>
</thead>

<tbody>
 <% @books.each do |book| %>
 <tr>
 	<td><%= link_to book.user do %>
 		<%= attachment_image_tag book.user, :profile_image, :fill, 150, 150, fallback: 'no-image-icon.jpg' %>
 		<% end %>
 	</td>
  <td>
   <%= link_to book.title, book_path(book.id) %>
  </td>
  <td>
   <%= book.body %>
  </td>
<% end %>



</tr>
</tbody>
</table>
</div>
</div>
</div>

